name = "projectcraft"
compatibility_date = "2026-07-09"
workers_dev = false
preview_urls = false

[vars]
# GitHub OAuth App client ID (non-sensitive — visible in authorize URLs).
# Used by the Decap OAuth proxy at src/pages/api/oauth.astro.
GITHUB_CLIENT_ID = "Ov23liWISCKJJvhWybtZ"

# GITHUB_CLIENT_SECRET is set via `wrangler secret put GITHUB_CLIENT_SECRET`
# (do NOT commit it here). Required by the proxy to exchange the auth code.

# Cloudflare Turnstile site key (public — visible in client HTML).
# Used by the Turnstile widget component.
TURNSTILE_SITE_KEY = "0x4AAAAAADylLKIuNpon54TO"
