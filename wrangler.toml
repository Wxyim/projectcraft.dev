name = "projectcraft"
compatibility_date = "2026-07-09"
workers_dev = false
preview_urls = false

# Decap CMS uses PKCE auth (client-side GitHub OAuth) — no server-side
# OAuth vars or secret are needed. See public/admin/config.yml.
